// const lines = [
//     '1 80 40'
// ];

// //keiji
// const [m, p, q] = lines[0].split(" ").map(Number);

// const fn = (i) => remainingAmount - remainingAmount * i / 100;

// let remainingAmount = m;
// remainingAmount = fn(p);
// remainingAmount = fn(q);

// console.log(remainingAmount);

// //suusan
// const [all,raw,processed] = lines[0].split(' ');
    
// const calculateRemains = (a,b,c) => a - a * b * 0.01  - (a -a * b * 0.01 ) * c * 0.01;
// console.log(calculateRemains(all,raw,processed));

// //tubasasan
// <?php
//     $input = explode(" ", trim(fgets(STDIN)));
//     [$purchase_amount, $nama_salse, $souzai_salse] = $input;
    
//     $unsold = $purchase_amount*(1-$nama_salse/100)*(1-$souzai_salse/100);
//     echo $unsold;
// ?>

// //shougokun
// onst [m, p, q] = lines[0].split(' ').map(Number);
  
//   const calc = (M, P, Q) => (M * (100-P) / 100) * (1 - Q / 100);
  
//   console.log(calc(m, p, q));
